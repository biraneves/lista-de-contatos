import { MainContainer } from '../../styles';
import ContactsList from '../ContactsList';

const ContactsArea = () => (
    <MainContainer>
        <h2>Contatos</h2>
        <ContactsList />
    </MainContainer>
);

export default ContactsArea;
